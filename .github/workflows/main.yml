name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run the tests
      run: |
        pip3 install pipenv
        pipenv install --dev
        pipenv run pytest
